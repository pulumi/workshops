name: itops-agent-aks-azure-openai
runtime:
  name: python
  options:
    virtualenv: venv
description: >-
  Step 1 — the output of `pulumi new azure-native-python`: an empty Pulumi
  program targeting Azure, before any infrastructure exists.
