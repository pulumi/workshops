---
apiVersion: v1
kind: Service
metadata:
  name: adk-agent
  namespace: adk
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: adk-agent
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
