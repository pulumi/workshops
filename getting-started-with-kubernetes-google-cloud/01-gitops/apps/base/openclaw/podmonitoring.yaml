---
apiVersion: monitoring.googleapis.com/v1
kind: PodMonitoring
metadata:
  name: gke-openclaw
  namespace: openclaw
spec:
  selector:
    matchLabels:
      app.kubernetes.io/instance: gke-openclaw
  endpoints:
    - port: metrics
      interval: 30s
      path: /metrics
